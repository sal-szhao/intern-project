import setuptools

setuptools.setup(
    name='internv2',
    version='2.0',
    description='version 2 of the intern project',
    packages=setuptools.find_packages(),
    python_requires='>=3.6',
)
